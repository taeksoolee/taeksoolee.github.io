<pre>
## 윈도우 명령어 모음

# 실행중인 port 표시, 프로세스id(pid) 표시
  $ netstat -a -o 

# port 죽이기
  $ taskkill /f /pid 1234

# 파일 / 디렉토리 삭제
  $ del [파일명]
  $ rmdir /s [디렉토리명]

# 키 별칭 alias
  $ doskey [별칭이름]=[명령어]

# 단어 검색 ( linux의 grep )
  $ findstr [찾을단어]

</pre>